name: Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint-black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: psf/black@stable
        with:
          options: "--check --verbose"
          src: "./src"

  lint-pylint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - name: Install dependencies
        run: uv sync
      - name: Lint
        run: uv run pylint src

  # check-translations:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #     - name: Validate translations
  #       run: cd i18n && python3 i18n.py validate

  # run-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         submodules: recursive
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v6
  #     - name: Install dependencies
  #       run: uv sync
  #     - name: Run tests
  #       run: uv run pytest --cache-clear tests

  # coverage:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         submodules: recursive
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v6
  #     - name: Install dependencies
  #       run: uv sync
  #     - name: Build coverage file
  #       run: uv run pytest --cache-clear --cov src --cov-report xml tests
  #     - name: Upload coverage reports to Codecov with GitHub Action
  #       uses: codecov/codecov-action@v5
  #       with:
  #         token: ${{ secrets.CODECOV_TOKEN }}
